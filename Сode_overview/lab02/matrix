-импортировал функцию нампай

-сделал обущю функцию для проверки на рваность матрицы
если матрица пустая, то просто выбрасывается из функции
если матрица рваная, то возвращает ошибку

1:
создал функцию по транпорированию матрицы
если она пустая, то просто возвращаю список
если не пустая, то перевожу в нампай массив по типу такого [[   ]
                                                            [   ]
                                                                  ]
.T - транспорирую 
.tolist() перевожу обратно в список списков

2:
функция по сложению строк
перевожу в нампай массив

.sum(axis = 1) склеивает столбцы и суммирует

3:
функция по сложению столбцов
перевожу в нампай массив

.sum(axis = 0) склеивает строки и суммирует





